PROGRAM gravitionalFormula
BEGIN
	FLOAT g, m1, m2, r, r2, f, m, weight;
	STRING newline := "\n";
	FUNCTION VOID main()
	BEGIN
		READ(m1, m2, r);
		m := m1*m2;
		r2 := r*r;
		g := 9.8;
		weight := g*m;
		f := weight/r2;		
		WRITE(f, newline);
	END
END

